# ShareYum Food API (Phase A2.1 skeleton)
PORT=8788

# Auth verifier: mock (dev/test only) | fadicore-stub (default outside mock)
# FOOD_API_AUTH_MODE=fadicore-stub
# MOCK_AUTH=true
# FOOD_API_MOCK_AUTH=true

# NODE_ENV=development
