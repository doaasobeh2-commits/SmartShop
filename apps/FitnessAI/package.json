{
  "name": "fitness-ai",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "shared",
    "core"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@fitness-ai/app",
    "build": "npm run build --workspace=@fitness-ai/app",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
